@model IEnumerable<PracticaFinalMod1_Web.CodigoActividad>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Codigo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Codigo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CUIL }) |
            @Html.ActionLink("Details", "Details", new { id=item.CUIL }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CUIL })
        </td>
    </tr>
}

</table>
